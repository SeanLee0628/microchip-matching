client_max_body_size 50M;
# Allow long-running requests (e.g. subul-filter on large workbooks).
# nginx default proxy_read_timeout is 60s -> caused 504 on big files.
proxy_read_timeout 300s;
proxy_send_timeout 300s;
